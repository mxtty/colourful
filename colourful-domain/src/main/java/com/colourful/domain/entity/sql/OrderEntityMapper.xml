<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.colourful.domain.entity.mapper.OrderEntityMapper">

<insert id="insert"  useGeneratedKeys="true" parameterType="com.colourful.domain.generated.record.BrnOrder">
    <selectKey keyProperty="orderId" resultType="long" order="BEFORE">
        select cast(nextval('order_id_seq') as LONG) 
    </selectKey>
    insert into
       BRN_ORDER 
    values
       (#{orderId},#{status},#{totalPrice},#{shipName},#{shipZip},#{shipCountry},#{shipCity},#{shipAddress},#{phone},#{cellPhone},#{email},#{crtcardNo},#{crtcardHolder},#{exprdate},#{cardtype},CURRENT_TIMESTAMP,CURRENT_TIMESTAMP,0)
</insert>
  
</mapper>